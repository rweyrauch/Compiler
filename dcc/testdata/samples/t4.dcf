class Program 
{
    int a;
    string[] b;

    int[] tester(int d) 
    {
        b = NewArray(1, string);
        return NewArray(d, int);
    }             

    void main() 
    {
        int a;
        boolean b;
        int[] d;
        
        a = 1;
        while (a < 5) 
        {
            double b;
            if (a % 2 == 0) 
            {
                d = tester(a);
                break;
            }
            callout("printf", "Loop %d\n", a);
            a = a + 1;
        }
        d[0] = 0;
        callout("printf", "%d\n%d\n", d[d[0]], d.length());
    }
}

