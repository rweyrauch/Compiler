class Program
{
    void main () 
    {
        int a, b, c;
        
        a = 1;
        b = 2;
        c = 2;

        if (a < b) { callout("printf", "%d < %d is correct\n", a, b); }
        else { callout("printf", "%d < %d is incorrect\n", a, b); }
        
        if (b < a) { callout("printf", "%d < %d is incorrect\n", b, a); }
        
        if (a <= b) { callout("printf", "%d <= %d is correct\n", a, b); }
        if (a > b) { callout("printf", "%d > %d is incorrect\n", a, b); }
        if (a >= b) { callout("printf", "%d >= %d is incorrect\n", a, b); }
      
        if (c < b) { callout("printf", "%d < %d is incorrect\n", c, b); }
        if (c <= b) { callout("printf", "%d <= %d is correct\n", c, b); }
        if (c > b) { callout("printf", "%d > %d is incorrect\n", c, b); }
        if (c >= b) { callout("printf", "%d >= %d is correct\n", c, b); }
        
        if (a == a) { callout("printf", "%d == %d is correct\n", a, a); }
        if (a != a) { callout("printf", "%d != %d is incorrect\n", a, a); }

        if (a == b) { callout("printf", "%d == %d is incorrect\n", a, b); }
        if (a != b) { callout("printf", "%d != %d is correct\n", a, b); }
        if (a != b) { callout("printf", "%d != %d is correct\n", a, b); }
       
    }
}
